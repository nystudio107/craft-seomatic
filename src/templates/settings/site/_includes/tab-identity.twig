{% from "seomatic/settings/_includes/macros.twig" import configWarning %}
{% import "_includes/forms" as forms %}

{% namespace "seomaticSite" %}
    {{ forms.textField({
        label: "Site Name"|t("seomatic"),
        instructions: "The public name of the website; this is appended to the page title by default. This is also used in the JSON-LD for the homepage, if the MainEntityOfPage is WebPage or WebSite."|t("seomatic"),
        id: "siteName",
        name: "siteName",
        value: site.siteName,
        warning: false,
    }) }}
    {{ forms.textField({
        label: "Site Alternate Name"|t("seomatic"),
        instructions: "An alternate public name for the website. This is used in the JSON-LD for the homepage, if the MainEntityOfPage is WebPage or WebSite."|t("seomatic"),
        id: "siteAlternateName",
        name: "siteAlternateName",
        value: site.siteAlternateName,
        warning: false,
    }) }}
{% endnamespace %}

{% set entity = site.identity %}
{% namespace "seomaticSite[identity]" %}

    {# ############## Main Entity of Page ############## #}

    {% include "seomatic/settings/_includes/mainEntityOfPage-partial.twig" with {
        "mainEntityOfPage": entity,
        "entitySchemaPath": seomatic.helper.entityPath(entity),
        "mainEntityTitle": "Site Owner Entity Type",
        "mainEntityDescription": "The type of entity that owns this website.",
    } %}

    {% include "seomatic/settings/site/_includes/entity-partial.twig" with {"pageContext": "identity"} %}
{% endnamespace %}
